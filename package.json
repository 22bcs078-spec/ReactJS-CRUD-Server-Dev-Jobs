{
  "name": "mock-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch jobs.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}